'use strict';

let __template = require('./template.html');

export default {
  template: __template,
  controller: __controller
} 

__controller.$inject = [
  '$log'
]

function __controller ($log) {
}